# 内容维护

## 最简单的发文方式（推荐）

双击根目录的“发文后台.bat”。浏览器会打开只在本机运行的发文页：直接粘贴第一行 `# 文章标题` 加后续正文，不需要 frontmatter，也不需要拆开标题和正文。填写自由分类、发布日期与文章摘要后，页面会实时显示链接与阅读预览；摘要会先从正文首段自动生成，也可以改成更适合文章列表、详情页、搜索结果与 RSS 展示的版本，留空时仍自动使用首段。点击“发布文章”并最终确认后，助手自动构建、只提交这篇文章并推送上线。新文章链接采用永久日期编号，例如 `2026-07-28-01`；同一天的后续文章依次为 `-02`、`-03`。链接不随标题或更新时间改变，完整标题、摘要、正文与 canonical URL 共同承担 SEO 信息。

文章分类没有固定列表。每篇文章里的 `category` 都可以是新名称，文章列表会自动出现相应的筛选项。发文后台在最终确认前不会创建文件、提交或推送；发布时只提交新文章文件，不混入其他本地改动。

也可以使用 `pnpm content:new article my-article 标题` 或 `pnpm content:new project my-project 项目名称` 创建不会覆盖既有内容的 Markdown 模板；添加 `--draft` 会保存到 `src/content/drafts/`，不参与生产构建。确认发布时，将文件移至对应的 `articles` 或 `projects` 目录后再构建。Windows 下可直接运行根目录的“启动内容管理.bat”“预览网站.bat”“构建网站.bat”“发布网站.bat”和“备份内容.bat”。

项目写在 `src/content/projects/`，文章写在 `src/content/articles/`。每个 Markdown 文件的 frontmatter 控制标题、slug、摘要、分类、日期、更新时间和主视觉，正文会自动生成独立静态页面。文章新增或实质修订后请更新 `updatedDate`；草稿只保存在本地，确认发布前再添加到内容目录并构建。

新增项目或文章后执行 `pnpm build`；图片放入 `public/assets/`，并在 frontmatter 中以 `/assets/文件名` 引用。首页精选项目由 `featured: true` 控制。
